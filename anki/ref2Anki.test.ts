import assert from 'assert';

describe("RefineryDb -> Anki", function () {
  it("should load highlights successfully", function () {

  });
  it("should transform highlights into a valid sqlite3 database used by Anki", function () {

  });
  it("should transform highlights into a valid apkg exported format used by Anki", function () {

  });
});